(document.onreadystatechange = () => {
    if (document.readyState === 'complete') {
        const model = {};
        const startView = {};
        const gameView = {};
        const endView = {};
        const controller = {};
    }
})();
